*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/app/build

# Signing credentials — never commit to public repos
*.keystore
*.jks

# gradle.properties IS tracked: it holds build settings the project cannot
# build without (android.useAndroidX and friends). Signing credentials must
# never go in it — put them in ~/.gradle/gradle.properties or pass them as
# ORG_GRADLE_PROJECT_* environment variables. See gradle.properties.example.

# Local AI assistant guidance — not tracked
CLAUDE.md
docs/superpowers/

# Codex bridge shared secret (see docs/face/desktop-codex-bridge.md)
tools/codex-bridge/.codex-bridge-token
