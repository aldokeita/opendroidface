<?xml version="1.0" encoding="utf-8"?>
<!--
  API 31+ draws the launch as a real splash screen rather than just a window
  background, so it gets its own attributes.

  The icon cannot be switched off - the platform always draws one - and it stays
  the launcher icon, which is the mark this app is recognised by on the home
  screen. What follows it is the name, so the launch reads mark, then name, then
  app. It used to be the robot mark and then a face, which looked like two
  different apps; the face is gone from the splash entirely now.
-->
<resources>
    <style name="Theme.OpenDroid" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:windowSplashScreenBackground">@color/window_background</item>
    </style>
</resources>
